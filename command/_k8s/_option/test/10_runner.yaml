##################################################################################################
# command-test
##################################################################################################
---
apiVersion: v1
kind: Service
metadata:
  name: command-test
  labels:
    app: command-test
    service: command
spec:
  ports:
  - port: 3000
    name: tcp
  selector:
    app: command-test
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: command-test
  labels:
    account: command-test
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: command-test
  labels:
    app: command-test
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: command-test
      version: v1
  template:
    metadata:
      labels:
        app: command-test
        version: v1
    spec:
      serviceAccountName: command-test
      securityContext:
        runAsUser: 0
        runAsGroup: 0
        fsGroup: 0
      containers:
        - name: command-test
          image: mcr.microsoft.com/playwright:v1.38.0-jammy
          imagePullPolicy: IfNotPresent
          resources:
            limits:
              cpu: 1000m
              memory: 1000Mi
            requests:
              cpu: 500m
              memory: 500Mi
          tty: true
          ports:
            - containerPort: 3000
          workingDir: /my-app
          volumeMounts:
          - name: command-test-app
            mountPath: /my-app
      volumes:
      - name: command-test-app
        persistentVolumeClaim:
          claimName: command-test-app
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: command-test-app
  labels:
    app: command-test
spec:
  storageClassName: command-test-app
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi